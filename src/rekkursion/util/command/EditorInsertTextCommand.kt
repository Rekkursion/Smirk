package rekkursion.util.command

import rekkursion.view.control.editor.CodeCanvas

class EditorInsertTextCommand(codeCanvas: CodeCanvas) : EditorCommand {
    override val mCodeCanvas = codeCanvas

    override fun execute() {

    }
}